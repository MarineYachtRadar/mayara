[workspace]
members = ["mayara-core", "mayara-server"]
resolver = "2"

# WASM release profile optimizations
[profile.release]
opt-level = "s"     # Optimize for size
lto = true          # Link-time optimization
codegen-units = 1   # Better optimization
